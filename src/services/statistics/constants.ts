// ─── Statistics Constants ────────────────────────────────────────────────────
// Shared values live in src/config.ts; re-exported here for local consumers.

export { MAX_HISTORY_SESSIONS as MAX_HISTORY } from '../../config';
